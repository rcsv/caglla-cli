## v0.7.0

### Added
- Extend `trip checklist-generate` with category combination rules
- Add `ChecklistRule` and `checklist_combination_rules()` for context-aware checklist items

### Changed
- Generate combination-based items after per-category `default_checklist`
- Update README with combination rule documentation

### Quality
- 89 tests passed
