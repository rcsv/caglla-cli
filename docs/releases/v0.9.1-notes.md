# v0.9.1 - JSON output polish

## Added

- Added `--json` output support to `itinerary list`.
- Added `--json` output support to `itinerary show`.
- Documented JSON output behavior and supported commands in README.

## Changed

- Reused the existing pretty JSON output helper for itinerary JSON output to keep CLI behavior consistent.
- Aligned `trip stats --json` to use the same `print_json()` helper as other read commands.

## Quality

- `cargo fmt` passed.
- `cargo clippy -- -D warnings` passed.
- `cargo test` passed: 114 passed, 0 failed.
- `make check` passed.

## Notes

This patch release extends JSON output to itinerary read commands and clarifies that `--json` writes pretty JSON only to stdout.
