# v4.4.8 — Travel Book presentation helper cleanup

## Summary

Small cleanup after Phase 1–3 helper extraction: remove Markdown syntax (`- ` bullet, `**bold**`) from `travel_book_presentation.rs` and move it to `markdown.rs`.

**No output behavior change** — Okinawa golden body unchanged except Colophon `Version:`.

## Changes

- `travel_book_category_detail_label` — returns `("種別", display_name)`; bullet in renderer
- `travel_book_reservation_heading` — returns `TravelBookReservationHeading { main_label, provider_suffix }`; bold in renderer

## Closes v4.4.x arc

Helper extraction Phase 1–3 + v4.4.8 cleanup completes the Travel Book presentation helper boundary. `TravelBookDocument` remains **deferred** until UI / Venue requirements.

Implementation plan: [v4.4.8-travel-book-presentation-helper-cleanup.md](../specifications/v4.4.8-travel-book-presentation-helper-cleanup.md)

## Non-goals

- `TravelBookDocument` prototype
- Markdown display rule changes
- Schema changes
