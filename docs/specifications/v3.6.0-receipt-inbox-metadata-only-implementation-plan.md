# v3.6.0 Receipt Inbox Metadata-only Implementation Plan

Caglla.Travel CLI **v3.6.0 候補** で **Receipt Inbox（metadata-only）** を実装するための計画です。

**Implementation Plan。** v3.6.0 候補として **metadata-only Receipt Inbox を実装済み**（release は別途）。Receipt image / `image_path` / OCR は **対象外**（[v3.5.0-receipt-inbox-concept-design.md](v3.5.0-receipt-inbox-concept-design.md) §7 deferred を継続）。

| ドキュメント | 役割 |
|---|---|
| [v3.5.0-receipt-inbox-concept-design.md](v3.5.0-receipt-inbox-concept-design.md) | Concept Design — **上書きしない** |
| **本書** | Implementation Plan — metadata-only Receipt Inbox |
| [export-schema.md](export-schema.md) | 現行 export v6 — v7 候補は本書 §7 |
| [expense-model.md](expense-model.md) | Expense / Actual の責務 |
| [estimate-model.md](estimate-model.md) | Estimate / Planned の責務 |

| 項目 | 内容 |
|---|---|
| リリース候補 | v3.6.0 — Receipt Inbox metadata-only |
| 前提 | v3.5.0（Receipt Inbox concept design — documentation-only リリース済み） |
| スコープ | **metadata-only** — 画像なし |
| DB schema | **実装時に変更** — `receipts` table 追加（本書は計画のみ） |
| trip export schema | **v7 候補** — `receipts[]` at trip level（本書は計画のみ） |
| 現行 v6 | import 互換維持。**現行 export は v7**（`receipts[]` at trip level） |

関連: [v3.4.0-itinerary-planned-vs-actual-implementation-plan.md](v3.4.0-itinerary-planned-vs-actual-implementation-plan.md) / [long-term-version-strategy.md](../long-term-version-strategy.md)

設計系列（想定）:

```text
Concept Design              → v3.5.0-receipt-inbox-concept-design.md（v3.5.0 リリース済み）
Implementation Plan         → 本書（v3.6.0 候補）
Entity Design               → 未着手（本書の DDL を詳細化）
Phase 1 Implementation      → **完了**（f15a0e5 / 796b894）
Post-Implementation Review  → **完了** — [v3.6.0-receipt-inbox-metadata-only-post-implementation-review.md](v3.6.0-receipt-inbox-metadata-only-post-implementation-review.md)
Release v3.6.0              → **完了** — [v3.6.0-notes.md](../releases/v3.6.0-notes.md)
```

---

## 1. Background

v3.4.0 では **Itinerary-level Planned vs Actual difference** を `trip export-md` に導入した。確定済み Estimate と Expense を前提とした derived 集計が Itinerary 単位で見えるようになった。

v3.5.0 では **Receipt Inbox concept design** を documentation-only release として正式化した。未整理の支払い証拠を Expense やダミー Itinerary に押し込まず、Estimate / Receipt / Expense / Difference の責務を分離した。

その後、**Receipt image handling** は明確に **deferred** とした。画像を扱い始めると、保存場所・パス・export/import・欠損・CUI 表示・Attachment / Photo 設計など、CLI / CUI の現在の責務を超える。

次の検討対象は、**画像を扱わない metadata-only Receipt Inbox 実装** である。

```text
Estimate   = planned amount
Receipt    = Expense 化待ちの未整理支払い候補
Expense    = confirmed actual spending
Difference = planned-vs-actual delta based on confirmed Expenses
```

**Release 前 lifecycle 単純化（2026）:** status は `unreviewed` / `ignored` のみ。`receipt link` / `linked` / `converted` / `linked_expense_id` / `itinerary_id` は user-facing から除外。

---

## 2. Goal

Itinerary に紐づかない **Expense 化待ちの未整理支払い候補** を、**画像なし** で一時保存できるようにする。

Receipt は Expense ではない。Receipt は Actual spending として **確定していない**。

| 目的 | 内容 |
|---|---|
| 金額のみ | 金額だけ分かる支払いを残す |
| 日付のみ | 日付だけ分かる支払いを残す |
| メモ | 「これなんだっけ？」というメモを残す |
| 日ヒント | 任意で `day_id` — 「この日に発生したっぽい」程度 |
| 除外 | 不要・重複・対象外を `ignored` にできる |
| 昇格 | Itinerary 判明時は **Expense に昇格**（`receipt convert` — future work） |

Receipt Inbox の価値は、**分からないものを分からないまま保存できること**（concept design 継続）。

---

## 3. Non-goals

v3.6.0 実装計画および初期実装候補で **含めない** 範囲:

```text
- Receipt image support
- image_path field
- image binary storage
- image export / import
- ZIP / archive handling
- OCR
- automatic receipt parsing
- generic Attachment / Photo model（Evidence / Attachment 共通レイヤーとして将来検討）
- Balance / Settlement
- Participant-based payment sharing
- Planned vs Actual への即時反映
- Day-level Planned vs Actual difference
- trip stats への反映
- trip stats --json への反映
- during-trip budget control
```

Receipt image handling、Attachment / Photo 汎用化、Day-level difference、Balance / Settlement は **引き続き defer**。

---

## 4. Data model proposal

### `receipts` table（実装済み — lifecycle 単純化後）

```sql
CREATE TABLE receipts (
  id INTEGER PRIMARY KEY,
  trip_id INTEGER NOT NULL,
  day_id INTEGER NULL,
  amount INTEGER NULL,
  currency TEXT NULL,
  occurred_date TEXT NULL,
  memo TEXT NULL,
  status TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

| カラム | 方針 |
|---|---|
| `trip_id` | **必須** — Receipt は Trip スコープ |
| `day_id` | 任意 — 同一 Trip 内 Day のみ（「この日に発生したっぽい」ヒント） |
| `amount` / `currency` | 任意 — 片方のみは **拒否**（§9） |
| `occurred_date` | 任意 — ISO 日付文字列候補 |
| `memo` | 任意だが実用上 **重要** |
| `status` | **必須** — §5（`unreviewed` / `ignored` のみ） |
| `image_path` | **持たない** |

**削除済み / deferred:** `itinerary_id`, `linked_expense_id`（旧 DB は migration で正規化）。

旧スキーマ（`itinerary_id` / `linked_expense_id` あり）からの migration: `linked` / `converted` status は `unreviewed` に正規化し、列を削除。

FK は Expense / Estimate と同型で **DB FK なし + アプリ側 cascade** を想定（Entity Design で確定）。

### 最低限の登録パターン例

```text
trip_id + memo
trip_id + amount + currency
trip_id + day_id + memo
trip_id + occurred_date + amount + currency + memo
```

### Trip 階層での位置づけ

```text
Trip
  ├─ Day
  │    └─ Itinerary
  │         ├─ Expense
  │         └─ Estimate
  └─ Receipt   ← Trip-level collection（Itinerary 配下には置かない）
```

---

## 5. Receipt status lifecycle

| status | Meaning |
|---|---|
| `unreviewed` | まだ Expense 化されていない未整理支払い候補 |
| `ignored` | 旅行費用ではない、重複、対象外 |

初期実装では **`unreviewed` を主役** とする。

遷移:

```text
unreviewed → ignored（receipt ignore）
unreviewed → （Inbox から解消）Expense 昇格（receipt convert — future work）
```

**除外:** `linked`, `converted` — Itinerary だけ紐づけて Actual にしない中間状態は持たない。

---

## 6. CLI command proposal

metadata-only。`--image` フラグは **存在しない**。

### 追加

```bash
caglla receipt add --trip okinawa --day 1 --amount 1700 --currency JPY --memo "これなんだっけ？"
```

`--trip` は trip id または識別子（既存 CLI 慣習に合わせる — Entity Design で確定）。

### 一覧

```bash
caglla receipt list --trip okinawa
caglla receipt list --trip okinawa --unreviewed
caglla receipt list --trip okinawa --status ignored
```

### 詳細

```bash
caglla receipt show 3
```

### 更新

```bash
caglla receipt update 3 --memo "おかんのお土産っぽい"
caglla receipt update 3 --amount 1700 --currency JPY
```

### 対象外

```bash
caglla receipt ignore 3 --memo "旅行費用ではない"
```

### 削除

```bash
caglla receipt delete 3
```

### future work — `receipt convert` / `promote`

```bash
caglla receipt convert 3 --itinerary 12 --title "お土産"
```

Expense 作成ロジックと接続するため **v3.6.0 では未実装**。Itinerary 判明時は Expense 昇格が自然な操作。

**削除済み:** `receipt link` — Itinerary だけ紐づけて Actual にしない操作は採用しない。

---

## 7. Export / import schema impact

Receipt を実装する場合、旅行データの一部となるため **export/import に含める** 方針が自然。

### 基本方針案

| 項目 | 方針 |
|---|---|
| 配置 | **Trip-level collection** — Itinerary 配下には置かない |
| schema | **v7 候補** — v6 から additive |
| 画像 | なし — JSON export のみで表現可能 |
| internal id | export しない（既存 entity と同型） |

### 構造案

```json
{
  "schema_version": 7,
  "trip": { },
  "days": [],
  "participants": [],
  "receipts": [
    {
      "day_ref": { "day_number": 1 },
      "amount": 1700,
      "currency": "JPY",
      "occurred_date": "2026-04-26",
      "memo": "これなんだっけ？",
      "status": "unreviewed"
    }
  ]
}
```

`day_ref` の具体形式は、既存 export の **day_number** 方式に合わせる。`itinerary_ref` / `linked_expense_ref` は **含めない**。

### import / validate-export / diff への影響

| コマンド | 影響 |
|---|---|
| `trip export` | v7 出力 — `receipts[]` 追加 |
| `trip import` | v7 受け入れ — v6 互換維持 |
| `trip validate-export` | v7 receipt フィールド検証追加 |
| `trip diff` | receipt 追加・変更・削除の検出 |

v6 export の import は **引き続き可能**（`receipts` 省略 = 空配列）。

### 注意点

* 内部 DB ID を export/import 参照に **使わない**
* `day_id` は export 上は **`day_ref` オブジェクト**（optional）で表現
* Receipt は Difference / stats に影響しない — diff はデータ変更検出のみ
* import 時: 旧 export の `linked` / `converted` status は `unreviewed` に正規化。validate-export では invalid

---

## 8. Planned vs Actual relationship

Receipt は Planned vs Actual difference に **直接反映しない**。

```text
Estimate → Planned
Expense  → Actual
Receipt  → Expense 化待ちの未整理支払い候補（Actual ではない）
```

Receipt は Actual ではない。v3.3.0 / v3.4.0 の Difference calculation（`Actual − Planned`）には **含めない**。

`trip stats` / `trip stats --json` / `trip export-md` の Planned / Actual / Difference 出力は **変更しない**（回帰テスト対象）。

### Future work — post-trip review 補助表示

将来的な post-trip review では補助情報として表示できる余地を残す（**本 release 非対象**）:

```text
Confirmed Actual: JPY 18,300
Unreviewed Receipts: JPY 1,700
Potential Actual: JPY 20,000
```

`Potential Actual` は参考表示であり、Difference の正本ではない。

---

## 9. Validation rules

実装時の候補ルール:

| # | ルール |
|---|---|
| 1 | `status` は **`unreviewed` / `ignored` のみ**（`linked` / `converted` は invalid） |
| 2 | `currency` は既存 Expense と **同一通貨ルール**（`money.rs` 再利用） |
| 3 | `amount` がある場合、`currency` **必須** |
| 4 | `currency` がある場合、`amount` **必須** |
| 5 | `day_id` は同一 Trip に属する Day のみ |
| 6 | `ignored` でも `amount` / `currency` / `memo` は保持可能 |
| 7 | `receipt add` は `trip_id` + 少なくとも `memo` または (`amount` + `currency`) を要求 |
| 8 | delete は **物理削除** |
| 9 | export JSON に `image_path` / `itinerary_ref` / `linked_expense_ref` キーがあっては **ならない** |

---

## 10. Test plan

実装時のテスト観点:

### CLI integration

| テスト | 内容 |
|---|---|
| `receipt add` | 最小パターン（memo のみ / amount+currency） |
| `receipt list` | trip 単位一覧 |
| `receipt list --unreviewed` | status フィルタ |
| `receipt show` | 詳細表示 |
| `receipt update` | memo / amount / currency |
| `receipt ignore` | status → ignored、amount / currency / memo 保持 |
| `receipt delete` | 削除 |
| `receipt link` | **存在しない** |
| invalid status (`linked` / `converted`) | 拒否 |
| invalid trip / day ref | 拒否 |
| amount / currency validation | 片方のみ拒否 |

### Export / import

| テスト | 内容 |
|---|---|
| export v7 includes receipts | `receipts[]` 出力 |
| import restores receipts | roundtrip |
| v6 import 互換 | `receipts` 省略 |
| validate-export v7 | receipt フィールド検証 |
| diff detects receipt changes | 追加・更新・削除 |

### 回帰

| テスト | 内容 |
|---|---|
| planned vs actual output | **変更なし** |
| trip stats human | **変更なし** |
| trip stats --json | **変更なし** |
| export-md Difference | **変更なし** |
| schema / model | `image_path` フィールド **不存在** |

### Unit

* `receipt.rs` CRUD + status 遷移
* cascade: `trip delete` で receipts 削除
* migration 冪等

---

## 11. Implementation scope proposal

### In scope（v3.6.0 — lifecycle 単純化後）

```text
receipts table + migration（day_id optional のみ）
receipt add / list / show / update / ignore / delete
status: unreviewed / ignored
metadata-only Receipt model（image_path なし）
export schema v7 — trip-level receipts[]
import / validate-export / diff support
unit tests + tests/receipt_cli.rs
docs 更新
```

### deferred / future work

```text
receipt convert / promote（Itinerary 判明 → Expense 昇格）
Evidence / Attachment 共通レイヤーによる画像証憑（Receipt / Expense 両方）
post-trip review auxiliary display
Potential Actual display
```

### Out of scope

```text
receipt image support
image_path
OCR / automatic parsing
Attachment / Photo
Balance / Settlement
Participant sharing
Planned vs Actual difference integration
Day-level difference
trip stats integration
during-trip budget control
```

---

## 12. Release plan

**v3.6.0 リリース済み**（tag `v3.6.0`）。

| 項目 | 状態 |
|---|---|
| Post-Implementation Review | [v3.6.0-receipt-inbox-metadata-only-post-implementation-review.md](v3.6.0-receipt-inbox-metadata-only-post-implementation-review.md) |
| Release notes | [v3.6.0-notes.md](../releases/v3.6.0-notes.md) |
| Cargo.toml / Cargo.lock | `3.6.0` |
| tag `v3.6.0` + GitHub Release | 完了 |

---

## Validation（実装完了時）

```bash
make check   # PASS 必須
```

手動:

```bash
caglla receipt add --trip 1 --amount 1700 --currency JPY --memo "これなんだっけ？"
caglla receipt list --trip 1 --unreviewed
caglla trip export 1   # schema_version: 7, receipts[] あり
```

---

## References

| 用途 | パス |
|---|---|
| Concept Design | [v3.5.0-receipt-inbox-concept-design.md](v3.5.0-receipt-inbox-concept-design.md) |
| Post-Implementation Review | [v3.6.0-receipt-inbox-metadata-only-post-implementation-review.md](v3.6.0-receipt-inbox-metadata-only-post-implementation-review.md) |
| Expense 責務 | [expense-model.md](expense-model.md) |
| Export schema | [export-schema.md](export-schema.md) |
| バージョン戦略 | [long-term-version-strategy.md](../long-term-version-strategy.md) |
