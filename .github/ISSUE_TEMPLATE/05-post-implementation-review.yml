name: Post-Implementation Review
description: 実装後の責務検証・改善候補の記録（設計フェーズ 5/6）
title: "[Post-Implementation Review] "
labels:
  - phase:post-implementation-review
  - type:hardening
body:
  - type: markdown
    attributes:
      value: |
        **Post-Implementation Review** — 実装が設計時の責務を満たしているか検証します。
        v1 Hardening 系列（v1.19–v1.23）と同様、**documentation-only release** も選択肢です。

  - type: input
    id: concept
    attributes:
      label: 対象概念
      placeholder: Participant
    validations:
      required: true

  - type: input
    id: implementation_issue
    attributes:
      label: 前提 Issue（Implementation）
      placeholder: "#"
    validations:
      required: false

  - type: textarea
    id: goals_check
    attributes:
      label: Goals 達成確認
      placeholder: |
        | 課題 | 実装 | 判定 |
        |---|---|---|
        | 同行者を Trip に登録 | participant CRUD | OK |
    validations:
      required: true

  - type: textarea
    id: non_goals_check
    attributes:
      label: Non-goals 遵守確認
    validations:
      required: true

  - type: textarea
    id: gaps
    attributes:
      label: 既知ギャップ・改善候補
      description: v1 完了を阻害しない項目として記録
    validations:
      required: false

  - type: textarea
    id: defer
    attributes:
      label: 意図的 defer（次 Milestone へ）
    validations:
      required: false

  - type: textarea
    id: doc_path
    attributes:
      label: 成果物ドキュメント（予定パス）
      placeholder: docs/specifications/participant-post-implementation-review.md
    validations:
      required: true
